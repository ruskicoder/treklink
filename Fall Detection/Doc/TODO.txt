===============================================================
  TREKLINK — FALL DETECTION MODEL + FIRMWARE TODO CHECKLIST
===============================================================
Legend: [ ] = pending  [x] = done  [~] = in progress  [!] = blocked

---------------------------------------------------------------
  PHASE 1 — DATASET PROCUREMENT
---------------------------------------------------------------
[ ] 1.1  Download SisFall (Kaggle, no signup needed)
         → Place at: Fall Detection/DATASET/sisfallData/
         → Expected structure: SA01/, SA02/, ..., SE15/ folders
            each containing .txt files like D01_SA01_R01.txt

[ ] 1.2  Download FallAllD — derived Kaggle version (preprocessed to 50Hz)
         → Place at: Fall Detection/DATASET/fallalldData/
         → Use waist sensor data only when preprocessing

[ ] 1.3  Download UMAFall (Figshare, CC BY 4.0, no registration)
         → Place at: Fall Detection/DATASET/umaFallData/
         → Use waist sensor subset only

[ ] 1.4  Submit KFall request form (1–2 day approval, needs institutional email)
         → Place at: Fall Detection/DATASET/kfallData/ once approved
         → Has pre-impact labels — trains model to detect BEFORE body hits ground

---------------------------------------------------------------
  PHASE 2 — TRAINING PIPELINE FIXES (no dataset needed)
---------------------------------------------------------------
[x] 2.1  Fix data_prepare_fall_detection.py
         → Now loads all 6 axes: ax, ay, az, gx, gy, gz
         → Auto-merges all datasets from DATASET/processed/<class>/

[x] 2.2  Fix train_fall_detection.py model input shape
         → Updated to (128, 6, 1) — accel + gyro

[x] 2.3  Add class weighting to training
         → sklearn compute_class_weight('balanced') + 2.0x falling_regular,
           2.5x falling_extreme boost
         → Wired into model.fit(class_weight=...)

[x] 2.4  Fix INT8 quantization
         → Full integer quantization with representative dataset generator
         → Sets inference_input_type and inference_output_type to tf.int8
         → Required for TFLite Micro on ESP32-S3

[x] 2.5  Export normalization stats alongside model
         → data_load.py computes mean+std from training data only
         → Saves to norm_stats.json automatically on every training run

[x] 2.6  Write preprocessing scripts for FallAllD, UMAFall, KFall
         → DATASET/preprocess_fallalld.py — sliding window, waist sensor only
         → DATASET/preprocess_umafall.py  — waist (Device==5), auto-detects delimiter
         → DATASET/preprocess_kfall.py    — 100Hz→50Hz downsample, pre-impact windowing
         → All output to same processed/<class>/ folder with dataset-prefixed names
         → Each has --inspect flag to verify column names before processing

[x] 2.7  Unified data loader via shared processed/ folder
         → data_prepare_fall_detection.py auto-merges all datasets in one pass
         → No separate merge step needed

---------------------------------------------------------------
  PHASE 3 — TRAINING (needs dataset)
---------------------------------------------------------------
[ ] 3.1  Run: python preprocess_sisfall.py
         (+ FallAllD and UMAFall preprocessors when ready)

[ ] 3.2  Run: python data_prepare_fall_detection.py

[ ] 3.3  Run: python data_split_fall_detection.py

[ ] 3.4  Run: python train_fall_detection.py --model CNN

[ ] 3.5  Share confusion matrix output + class-level sensitivity scores
         → Need sensitivity > 95% on falling_regular + falling_extreme
         → FPR target < 11% (IMU-only baseline from paper C5)

[ ] 3.6  Retrain with adjustments if sensitivity target not met
         → Tune class weights, dropout, epochs

---------------------------------------------------------------
  PHASE 4 — MODEL EXPORT (needs trained model)
---------------------------------------------------------------
[ ] 4.1  Verify quantized model file size < 50KB
         → Reference: paper A1 achieved 9,623 bytes

[ ] 4.2  Run xxd to convert .tflite to C array
         → Output: magic_wand_model_data.cpp + .h
         → Drop into: Fall Detection/MagicWand-TFLite-ESP32-MPU6050/
                       magic_wand_esp32_mpu6050/

[ ] 4.3  Hardcode norm_stats.json values into accelerometer_handler or
         a new normalization header file for firmware

---------------------------------------------------------------
  PHASE 5 — ESP32-S3 FIRMWARE VARIANT (no dataset needed)
---------------------------------------------------------------
[ ] 5.0  USER: Confirm which ESP32-S3 module/board will be used
         (e.g. ESP32-S3-WROOM-1, Heltec S3, LILYGO S3, bare module)
         GPIO pinout in variant.h depends on this.

[~] 5.1  Create variants/esp32s3/treklink/ folder with:
         → variant.h  (GPIO pinout for S3 — pending board confirmation)
         → platformio.ini  ([env:treklink-s3] build config)
         → pins_arduino.h

[~] 5.2  Key S3 differences to handle vs current ESP32 variant:
         → SPI pins not fixed — need explicit GPIO assignment
         → ADC1_GPIO36 doesn't exist on S3 — new battery ADC pin
         → Native USB (USB-OTG) — serial monitor config differs
         → LED likely Neopixel on S3 devboards (not plain GPIO)
         → LEDC API same, buzzer PWM unaffected

---------------------------------------------------------------
  PHASE 6 — MODEL INTEGRATION INTO FIRMWARE (no dataset needed)
---------------------------------------------------------------
[~] 6.1  Integrate TFLite Micro inference into FallDetectionModule.cpp
         → Replace hardcoded threshold state machine with two-stage logic:
           Stage 1: magnitude threshold gate (keep — cheap, saves battery)
           Stage 2: run quantized 1D-CNN on 128-sample window
           Stage 3: vote filter (3/5 windows) before SOS trigger

[~] 6.2  Add 128-sample circular buffer to FallDetectionModule
         → Accumulate MPU6050 samples at 50Hz
         → Trigger Stage 2 inference on each full window

[~] 6.3  Wire model confidence thresholds to SOS trigger
         → falling_regular or falling_extreme confidence > 0.85 → vote++
         → 3 votes → trigger existing SOS/buzzer/GPS broadcast flow

[~] 6.4  Keep existing SOS cancel flow (10s buzz → button cancel)
         → No changes needed to BuzzerManager or ExternalNotificationModule

---------------------------------------------------------------
  PHASE 7 — TESTING (no hardware needed)
---------------------------------------------------------------
[~] 7.1  Unit test: run inference on saved SisFall test windows offline
         → Feed raw .tflite model with known fall/ADL samples
         → Verify class predictions match expected labels

[ ] 7.2  On-device test (needs hardware):
         → Walk, run, sit → no alert
         → Simulate fall onto mat → fall class fires within 600ms
         → SOS cancel: fall detected → buzz 10s → press button → cancelled

---------------------------------------------------------------
  NOTES
---------------------------------------------------------------
- Model input:  128 samples × 6 axes @ 50Hz = 2.56s window
- Model target: < 50KB quantized, < 100ms inference on ESP32-S3
- Sensitivity target: > 95% recall on fall classes
- Two-stage gate saves battery (Stage 2 only runs after >2g spike)
- Normalization: z-score per axis — stats MUST match training stats

===============================================================
